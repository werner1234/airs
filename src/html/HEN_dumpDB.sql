CREATE TABLE `portefeuilleOpbouw` (
  `id` int(11) NOT NULL auto_increment,
  `Clientvermogensbeheerder` varchar(10) NOT NULL,
  `Portefeuille` varchar(12) NOT NULL,
  `Soort` varchar(15) NOT NULL,
  `Categorie` varchar(20) NOT NULL,
  `Regio` varchar(30) NOT NULL,
  `Aantal` decimal(10,2) NOT NULL,
  `Instrument` varchar(20) NOT NULL,
  `ISIN` varchar(20) NOT NULL,
  `Omschrijving` varchar(60) NOT NULL,
  `Valuta` varchar(5) NOT NULL,
  `Begin_koers` decimal(10,2) NOT NULL,
  `Begin_waarde` decimal(10,2) NOT NULL,
  `Actueel_koers` decimal(10,2) NOT NULL,
  `Actueel_waarde` decimal(10,2) NOT NULL,
  `Actueel_aandeel` decimal(10,2) NOT NULL,
  `Koersresultaat` decimal(10,2) NOT NULL,
  `Valutaresultaat` decimal(10,2) NOT NULL,
  `Totaalresultaat` decimal(10,2) NOT NULL,
  `ResultaatPercent` decimal(10,2) NOT NULL,
  `change_user` varchar(10) default NULL,
  `change_date` datetime default NULL,
  `add_user` varchar(10) default NULL,
  `add_date` datetime default NULL,
  PRIMARY KEY  (`id`)
)
#
CREATE TABLE `portefeuillePerformance` (
  `Id` int(11) NOT NULL auto_increment,
  `Clientvermogensbeheerder` varchar(10) NOT NULL,
  `Portefeuille` varchar(12) NOT NULL,
  `Datum` date NOT NULL,
  `PortefeuilleIndex` decimal(10,2) NOT NULL,
  `IndexKoers` decimal(10,2) NOT NULL,
  `change_user` varchar(10) default NULL,
  `change_date` datetime default NULL,
  `add_user` varchar(10) default NULL,
  `add_date` datetime default NULL,  PRIMARY KEY  (`Id`)
)
#
CREATE TABLE `vermogensverloop` (
  `Id` int(11) NOT NULL auto_increment,
  `Clientvermogensbeheerder` varchar(10) NOT NULL,
  `Portefeuille` varchar(12) NOT NULL,
  `Startdatum` date NOT NULL,
  `Einddatum` date NOT NULL,
  `Risicodragend_startdatum` decimal(10,2) NOT NULL,
  `Risicodragend_einddatum` decimal(10,2) NOT NULL,
  `Risicodragend_onttrekkingen_stortingen` decimal(10,2) NOT NULL,
  `Risicodragend_gemiddeld_vermogen` decimal(10,2) NOT NULL,
  `Risicodragend_bruto_resultaat` decimal(10,2) NOT NULL,
  `Risicodragend_bruto_rendement` decimal(10,2) NOT NULL,
  `Risicodragend_weging` decimal(10,2) NOT NULL,
  `Risicodragend_bijdrage_rendement` decimal(10,2) NOT NULL,
  `Risicomijdend_startdatum` decimal(10,2) NOT NULL,
  `Risicomijdend_einddatum` decimal(10,2) NOT NULL,
  `Risicomijdend_onttrekkingen_stortingen` decimal(10,2) NOT NULL,
  `Risicomijdend_gemiddeld_vermogen` decimal(10,2) NOT NULL,
  `Risicomijdend_bruto_resultaat` decimal(10,2) NOT NULL,
  `Risicomijdend_bruto_rendement` decimal(10,2) NOT NULL,
  `Risicomijdend_weging` decimal(10,2) NOT NULL,
  `Risicomijdend_bijdrage_rendement` decimal(10,2) NOT NULL,
  `Liquiditeiten_startdatum` decimal(10,2) NOT NULL,
  `Liquiditeiten_einddatum` decimal(10,2) NOT NULL,
  `Liquiditeiten_onttrekkingen_stortingen` decimal(10,2) NOT NULL,
  `Liquiditeiten_gemiddeld_vermogen` decimal(10,2) NOT NULL,
  `Liquiditeiten_bruto_resultaat` decimal(10,2) NOT NULL,
  `Liquiditeiten_bruto_rendement` decimal(10,2) NOT NULL,
  `Liquiditeiten_weging` decimal(10,2) NOT NULL,
  `Liquiditeiten_bijdrage_rendement` decimal(10,2) NOT NULL,
  `Totaal_startdatum` decimal(10,2) NOT NULL,
  `Totaal_einddatum` decimal(10,2) NOT NULL,
  `Onttrekkingen_stortingen_totaal` decimal(10,2) NOT NULL,
  `Gemiddeld_vermogen_totaal` decimal(10,2) NOT NULL,
  `Bruto_resultaat_totaal` decimal(10,2) NOT NULL,
  `Totaal_bruto_rendement` decimal(10,2) NOT NULL,
  `Totaal_bijdrage_rendement` decimal(10,2) NOT NULL,
  `Niet_toe_te_rekenen_kosten` decimal(10,2) NOT NULL,
  `Netto_resultaat_totaal` decimal(10,2) NOT NULL,
  `Rendement_totaal` decimal(10,2) NOT NULL,
  `Rendement_benchmark` decimal(10,2) NOT NULL,
  `Netto_stortingenEnOntrekkingen` decimal(10,2) NOT NULL,
  `Netto_resultaat` decimal(10,2) NOT NULL,
  `Netto_netto_rendement` decimal(10,2) NOT NULL,
  `change_user` varchar(10) default NULL,
  `change_date` datetime default NULL,
  `add_user` varchar(10) default NULL,
  `add_date` datetime default NULL,  PRIMARY KEY  (`Id`)
)
#
CREATE TABLE `performanceMeting` (
  `Id` int(11) NOT NULL auto_increment,
  `Clientvermogensbeheerder` varchar(10) NOT NULL,
  `Portefeuille` varchar(12) NOT NULL,
  `KoersresultaatVerkopen` decimal(10,2) NOT NULL,
  `ValutaresultaatVerkopen` decimal(10,2) NOT NULL,
  `Uitkeringen` decimal(10,2) NOT NULL,
  `Belastingen` decimal(10,2) NOT NULL,
  `Rente` decimal(10,2) NOT NULL,
  `TotaalGerealiseerd` decimal(10,2) NOT NULL,
  `KoersresultaatPositie` decimal(10,2) NOT NULL,
  `ValutaresultaatPositie` decimal(10,2) NOT NULL,
  `TotaalOngerealiseerd` decimal(10,2) NOT NULL,
  `Valutaresultaat` decimal(10,2) NOT NULL,
  `DebetRenteLiquiditeiten` decimal(10,2) NOT NULL,
  `CreditRenteLiquiditeiten` decimal(10,2) NOT NULL,
  `TotaalOverig` decimal(10,2) NOT NULL,
  `TransactieKosten` decimal(10,2) NOT NULL,
  `Beheerloon` decimal(10,2) NOT NULL,
  `Bewaarloon` decimal(10,2) NOT NULL,
  `Performancefee` decimal(10,2) NOT NULL,
  `Bankkosten` decimal(10,2) NOT NULL,
  `RestitutieRetrocessie` decimal(10,2) NOT NULL,
  `TotaalKosten` decimal(10,2) NOT NULL,
  `TotaalResultaatInEuro` decimal(10,2) NOT NULL,
  `change_user` varchar(10) default NULL,
  `change_date` datetime default NULL,
  `add_user` varchar(10) default NULL,
  `add_date` datetime default NULL,
  PRIMARY KEY  (`Id`)
)
#
CREATE TABLE `Clienten` (
  `Id` int(11) NOT NULL auto_increment,
  `Clientvermogensbeheerder` varchar(10) NOT NULL,
  `Portefeuille` varchar(12) NOT NULL,
  `KlantNaam` varchar(50) NOT NULL,
  `KlantNaam1` varchar(50) NOT NULL,
  `Risicoklasse` varchar(50) NOT NULL,
  `VermogensbeheerOrg` varchar(10) NOT NULL,
  `AccountManager` varchar(15) NOT NULL,
  `change_user` varchar(10) default NULL,
  `change_date` datetime default NULL,
  `add_user` varchar(10) default NULL,
  `add_date` datetime default NULL,
  PRIMARY KEY  (`Id`)
)
#
CREATE TABLE `isin` (
  `Id` int(11) NOT NULL auto_increment,
  `ISIN` varchar(20)   NOT NULL,
  `omschrijving` varchar(60)   NOT NULL,
  `change_user` varchar(10)   default NULL,
  `change_date` datetime default NULL,
  `add_user` varchar(10)   default NULL,
  `add_date` datetime default NULL,
  PRIMARY KEY  (`Id`)
)
